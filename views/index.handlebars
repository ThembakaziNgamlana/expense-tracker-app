<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>Expense Tracker</title>
</head>
<body>
    <div id="app">
        <h1>Expense Tracker</h1>
        <div id="expense-form">
            
       <form id="expense-form" action="/expenses" method="POST">
    <div>
        <label for="amount">Amount:</label>
        <input type="number" id="amount" min="0" name="amount" required>
    </div>
    <br>
    <div>
        <label for="Description">Description:</label>
        <input type="text" id="Desc" name="description">
    </div>
    <br>
    <div>
        <label for="category">Category:</label>
        <select id="category" name="categoryId">
            <option value="monthly">Monthly</option>
            <option value="weekly">Weekly</option>
            <option value="weekday">Weekday</option>
            <option value="weekend">Weekend</option>
            <option value="once-off">Once-off</option>
            <option value="daily">Daily</option>
        </select>
    </div>
    <br>
    <button type="submit">Add Expense</button>
</form>


        <div id="expense-list">
            <h2>Expense List</h2>
            <ul id="expenses"></ul>
            <button onclick="removeAllExpenses()">Remove All Expenses</button>
        </div>

        <div id="totals">
            <h2>Expense Totals</h2>
            <p id="overall-total">Overall Total: {{expenseTotal}}</p>
            <p id="monthly-total">Monthly Total: $0</p>
            <p id="weekly-total">Weekly Total: $0</p>
            <p id="weekday-total">Monthly Total: $0</p>
            <p id="daily-total">Weekly Total: $0</p>
        </div>
    </div>

    <script src="app.js"></script>
</body>
</html>
